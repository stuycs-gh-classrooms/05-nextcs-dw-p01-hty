class Cell{
  PVector pos;
  int size;
  int state;
  
  Cell(x, y, sz, st){
     pos = new PVector(x, y);
     size = sz;
     state = st; 
  }
  
  void display(){
   square(pos.x,pos.y,sz);
   if(this.state = 1){
   }
  }
  void stateChange(){
    if state
  }
  
  
  
  
  
  
  
  
}
